import CreateShelterScreen from "@/components/CreateShelterScreen";
import { SafeAreaView } from "react-native-safe-area-context";

const CreateScreen = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <CreateShelterScreen />
    </SafeAreaView>
  );
};

export default CreateScreen;
